<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.gymhelp">
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false">
    </uses-feature>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icons8_dumbbell_48"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.example.android.gymhelp.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!--<activity
            android:name=".CuttingActivity"
            android:label="Cutting"
            android:parentActivityName=".MainActivity">

            &lt;!&ndash; Parent activity meta-data to support 4.0 and lower &ndash;&gt;
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".ChestActivity"
            android:label="@string/title_activity_chest"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BackActivity"
            android:label="@string/title_activity_back"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LegsActvity"
            android:label="@string/title_activity_legs_actvity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ArmsActivity"
            android:label="@string/title_activity_arms"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ShouldersActivity"
            android:label="@string/title_activity_shoulders"
            android:theme="@style/AppTheme.NoActionBar"></activity>-->
    </application>

</manifest>